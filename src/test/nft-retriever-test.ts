import assert from 'assert';
  
describe("NFT Retriver", () => {

    before(async () => {

    });

    after(async () => {

    });
      
    it("Doesn't have test yet", () => {
        assert.equal(1, 1);
    });
});